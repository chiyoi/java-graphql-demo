type Query {
  items: [Item!]!
  item(id: ID!): Item
}

type Mutation {
  createItem(text: String!): Item!
  updateItem(id: ID!, text: String, completed: Boolean): Item!
  deleteItem(id: ID!): Item!
}

type Item {
  id: ID!
  text: String!
  completed: Boolean!
}
